@model IEnumerable<PaymentOrders.ServiceReference1.PaymentOrder>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.OrigConID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PaymentNotes)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RecipientID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RemitterID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TransactionID)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.OrigConID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PaymentNotes)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RecipientID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RemitterID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TransactionID)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID })
        </td>
    </tr>
}

</table>
